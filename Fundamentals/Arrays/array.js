let students = [
    "Luigi",
    "John",
    "Maria",
    "Peter",
    "Anna"
];

console.log(students);
console.log(students[0]); // Luigi

// map
let numbers = [1, 2, 3];
const doubleNumber = numbers.map(number => {
    return number * 2;
})